package com.entertainment;/*
 * There are five instances of displayTypes, listed below, not strings
 *
 * enums automatically creates .valueoOf() and .value()
 */

public enum DisplayType {
    LED, OLED, PLASMA, LCD, CRT
}